# Verify

Use the `verify` skill for the current change. Run deterministic dependency and configured verification checks and report their exact status.

After the checks, record structured acceptance and final security evidence through the runtime, then run `python .vibe/tools/vibe.py complete --summary`. Keep `PASS_VERIFIED` separate from final `COMPLETE`.
