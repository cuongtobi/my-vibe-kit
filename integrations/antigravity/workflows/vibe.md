# Vibe

Use the `vibe` skill for the user's request.

Classify the task, run plan -> build -> verify -> complete, keep scope minimal, and report actual verification evidence. Record structured acceptance and final security evidence before the completion gate. Do not skip dependency/impact analysis for non-trivial changes.
