# Vibe

Use the `vibe` skill for the user's request.

Classify the task, run plan -> build -> verify, keep scope minimal, and report actual verification evidence. Do not skip dependency/impact analysis for non-trivial changes.
